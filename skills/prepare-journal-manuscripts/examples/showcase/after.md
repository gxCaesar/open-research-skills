# Corrected synthetic teaching excerpt

This is not a scientific manuscript or an accepted paper.

## Results

Six constructed specimens each contribute one baseline and one follow-up value.
Follow-up exceeds baseline for four specimens and is lower for two.
These illustrative values demonstrate paired reporting only. They do not support a
treatment effect, population generalization or causal claim.

## Methods

Each row of `observations.csv` identifies one synthetic specimen and two paired values.
All six rows are displayed; none is excluded. No model, hypothesis test, interval estimate
or population inference is performed.

## Figure legend

**Synthetic paired observations.** Each point represents one constructed specimen
(`n = 6` specimens, two measurements per specimen). The horizontal axis shows baseline
response and the vertical axis shows follow-up response in arbitrary units. Points are
individual paired observations, not a group mean. No fit, error bar or statistical test
is shown. All values come from `observations.csv`.

## Data availability

The six-row synthetic source data are supplied as `observations.csv` alongside this
teaching example. No external deposit or accession is claimed.

## Limitations

The example contains no biological material, sampled population, intervention or
independent validation. Passing its reporting checks establishes record completeness
only, not statistical validity or readiness for a journal.
